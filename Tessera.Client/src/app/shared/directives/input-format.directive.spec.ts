import { InputFormatDirective } from './input-format.directive';

describe('InputFormatDirective', () => {
  it('should create an instance', (arg : any) => {
    const directive = new InputFormatDirective(arg);
    expect(directive).toBeTruthy();
  });
});
