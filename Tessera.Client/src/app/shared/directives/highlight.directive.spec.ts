import { HighlightDirective } from './highlight.directive';

describe('HighlightDirective', () => {
  it('should create an instance', (arg : any) => {
    const directive = new HighlightDirective(arg);
    expect(directive).toBeTruthy();
  });
});
